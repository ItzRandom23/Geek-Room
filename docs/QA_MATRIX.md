# PitSense AI QA Matrix

Evidence captured on 2026-08-15 from the repository checkout. “Demo” means the explicit seeded fixture, not a fabricated live inference result.

| Area | Test | Expected result | Actual result | Status | Evidence |
|---|---|---|---|---|---|
| Startup | Backend health | API starts on port 8000 and `/api/health` is OK | `python -m uvicorn app.main:app --host 127.0.0.1 --port 8000`; health returned `{"status":"ok"}` | PASS | Live HTTP request |
| Startup | Frontend dev server | Next app starts on port 3000 | `npm.cmd run dev`; homepage returned HTTP 200 | PASS | Live HTTP request |
| Build | Frontend production build | TypeScript and static generation complete | `npm.cmd run build` completed; 9 routes generated | PASS | Build output |
| Quality | Frontend type/lint command | Configured quality command runs successfully | `npm.cmd run lint` now executes `tsc --noEmit` successfully under Next 16 | PASS | Terminal output |
| Security | Frontend dependency audit | Production dependency graph has no known advisory | `npm.cmd audit --omit=dev` returned `found 0 vulnerabilities`; full audit retains development-tool advisories documented in the final audit | PASS / LIMITED | npm audit output |
| Routes | `/`, `/login`, `/onboarding`, `/sessions`, `/sessions/{demo_id}`, `/analytics`, `/methodology` | Page renders without a blank/error screen | All loaded in the browser after hook fixes; demo ID is generated by seed | PASS | Browser route sweep |
| Routes | `/settings` while logged out | Safe redirect to team access | Redirected to `/login` | PASS | Browser route sweep |
| Auth | Registration | Account and organization are created | Unique local QA account registered and routed to onboarding | PASS | Browser flow |
| Auth | Onboarding | Role selection and completion work | 3 steps completed; routed to `/sessions` | PASS | Browser flow |
| Auth | Invalid login | Human-readable error; form remains usable | “Incorrect email or password.” shown on `/login` | PASS | Browser flow |
| Auth | Logout and re-login | Token clears; valid credentials work again | `/settings` logout returned to `/login`; re-login returned to `/sessions` | PASS | Browser flow |
| Settings | Browser-local preference | Saved preference remains after refresh | Weekly digest remained checked after settings refresh and reopening Preferences | PASS | Browser flow |
| Security | Organization isolation | User cannot read another organization’s session | Existing `test_authentication_isolates_organizations` passed | PASS | `backend/tests/test_production.py` |
| Security | Protected settings API | Unauthenticated update rejected | Settings page redirects; endpoint requires auth | PASS | Browser + backend implementation |
| Core flow | Demo entry | CTA reaches the explicit fixture | Landing CTA reached `/sessions?demo=1` and followed the generated demo ID | PASS | 3 browser runs |
| Core flow | Demo report | Meaningful transcript, vocal state, lap correlation, and recommendations render | Report showed English source language, urgent evidence, 8 real laps, and recommendations | PASS | Browser DOM/screenshot |
| Core flow | Evidence selection | Selecting urgent evidence updates the review state | Critical urgent evidence control was present and clickable in all 3 runs | PASS | Browser flow |
| Core flow | Normal upload validation | Bad audio/lap inputs are rejected before processing | Backend validation, upload, CSV, and mutation-lock tests passed | PASS | 46 backend tests |
| Recovery | PDF without WeasyPrint | Export remains a valid PDF | Fallback returned `application/pdf` and `%PDF-` header | PASS | `test_audio_only_does_not_create_or_claim_laps` |
| Persistence | Seed reset | Re-running seed is idempotent | Second `python -m app.seed_demo` returned existing demo id without error | PASS | Terminal output |
| Persistence | Scoped demo reset | Reset removes/recreates only the labelled fixture | `python -m app.seed_demo --reset` recreated demo session 14; final browser runs used it | PASS | Terminal + browser |
| Persistence | Refresh | Authenticated and demo pages survive reload | Demo page reloaded with report and English metadata | PASS | Browser flow |
| Persistence | Test isolation | Automated tests do not pollute the judge database | Tests use disposable `backend/pitsense_test.db` and `backend/test-uploads`; both are removed at session end; dashboard contains only the demo fixture afterward | PASS | `backend/tests/conftest.py` + database check |
| Analytics | Historical summary | Dashboard/analytics show useful report totals | Analytics showed the seeded demo row with 5 reportable events and 8 laps | PASS | Browser DOM |
| Analytics | Filtered export | Visible export action works | Button is enabled and builds a client-side CSV from filtered rows | PASS | Source review/build; browser click did not expose a download event |
| UI | Desktop | No visible broken layout | Demo screen rendered correctly at default desktop viewport | PASS | Screenshot |
| UI | Mobile 390×844 | No horizontal overflow or clipped primary content | `scrollWidth` 375, `innerWidth` 390; no overflow | PASS | Browser evaluation + screenshot |
| Quality | Browser console | No blocking runtime errors | Fresh browser QA tab reported zero error-level logs | PASS | `tab.dev.logs` |
| UI | Tablet 768x900 | Responsive controls remain usable without overflow | `scrollWidth` 753, `innerWidth` 768; no overflow | PASS | Browser evaluation |
| Demo | Complete run, repeated three times | Landing -> demo -> report -> evidence -> analytics -> methodology in under three minutes | Run 1: 10.51s; Run 2: 10.47s; Run 3: 10.50s; all report/analytics/methodology checks passed | PASS | Fresh browser automation |
| Runtime | Bounded local concurrency smoke | Read-only health/report traffic remains responsive with request IDs and no 5xx responses | On local backend port 8001, 50 concurrent `/api/health` requests and 20 concurrent `/api/sessions/14/report` requests returned 200; combined elapsed time 0.208s | PASS / LIMITED | PowerShell/Python HTTP smoke run; not production-scale load validation |
| External dependency | Live HF inference | Normal uploads complete with configured models | Not run: model weights/network are external; demo fixture intentionally avoids this dependency | PARTIAL | Documented limitation |
| Deployment | Docker Compose | Container stack starts | Not run: Docker is not installed in the verification environment | BLOCKED | `docker --version` command not found |

## Automated commands

```text
cd backend && python -m pytest -q       -> 46 passed, 1 deprecation warning
cd frontend && npm.cmd test             -> 9 passed
cd frontend && npm.cmd run lint         -> passed (`tsc --noEmit`)
cd frontend && npm.cmd run build        -> passed
```

The warning is Starlette’s compatibility notice for the installed `httpx` version; it does not fail the suite.
## Backend route inventory

FastAPI route enumeration was generated directly from `app.routes`. Health/readiness/model/status/benchmark/session/timeline/report/export GET paths returned successful responses against the seeded fixture. Auth, onboarding, session mutation, upload, lap, job, retry, cancellation, and organization paths were exercised through the browser and/or the 46-test backend suite; protected paths returned safe auth errors when unauthenticated. OpenAPI documentation routes (`/docs`, `/redoc`, `/openapi.json`) are framework-generated and loaded as expected.

Registered application route families:

```text
GET  /api/health /api/readiness /api/models/status /api/models/benchmark
POST /api/auth/register /api/auth/login /api/onboarding/complete
GET/PATCH /api/me; POST /api/me/password; GET /api/organizations
GET/POST/DELETE /api/sessions and /api/sessions/{id}
POST /api/sessions/{id}/audio and /audio/{clip_id}/replace
GET/DELETE /api/sessions/{id}/audio/{clip_id}/file and /audio/{clip_id}
GET/POST /api/sessions/{id}/laps, /laps/csv, /laps/manual
POST /api/sessions/{id}/analyse and /analysis/cancel
GET /api/jobs/{job_id}; POST /api/jobs/{job_id}/retry
GET /api/sessions/{id}/timeline, /report, and report.json/csv/pdf exports
```
