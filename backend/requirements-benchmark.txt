-r requirements.txt
# Install only in the CUDA benchmark environment. The web/API runtime keeps
# these optional adapters lazy so it can continue serving the baseline model.
datasets==3.2.0
huggingface-hub==0.27.0
speechbrain==1.0.2
funasr==1.1.14
modelscope==1.20.0
torchaudio==2.6.0
